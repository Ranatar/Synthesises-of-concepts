# Ревизия описаний связей philosophy_graph.html — рабочая папка

**РАБОТА ЗАВЕРШЕНА ЦЕЛИКОМ.** Здесь всё, что наработано за сорок заходов; исключены только `node_modules` (ставятся по `tools/package.json`).

**Начинать здесь:** `reports/report-summary.md` — сводный отчёт по всей работе.
Полные формулировки критериев — `spec/descriptions-revision-spec.md` (ревизия 4).
`INSTRUCTIONS-next-batch.md` сохранён как описание порядка работ.

## Что сделано

| Слой | Связей | Состояние |
|---|---|---|
| Внутрисистемный | 662 | завершён (заходы 0–12) |
| Межсистемный | 953 | завершён (заходы M01–M18) |
| Поправка строя `fix` | 224 из 224 | заходы C01–C05, завершена |

**Описаны все 1615 связей** (в исходнике было 1051). Проверка гоняется
**без исключений**: `known-exceptions.json` опустошён, отказов 0, мутации 8 из 8.
Единый проход типографики не понадобился — заменять оказалось нечего.
Текущий файл: `dist/philosophy_graph.html`, md5 `26d9eea9ea44e65863df42c0fd6d5b61`.
Исходник: `source/philosophy_graph.SOURCE.html`, md5 `ce1e5cc8d8d1fc429a07882994a0ccd3`.

## Как воспроизвести текущий файл

Все команды — из корня этой папки.

```bash
cp source/philosophy_graph.SOURCE.html work.html
# ВНИМАНИЕ: ls с несколькими шаблонами сортирует всё вместе и ставит C01
# перед M01. Вызовы обязаны быть раздельными.
for f in $(ls edits/batch-0*.json) $(ls edits/batch-1*.json) \
         $(ls edits/batch-M*.json) $(ls edits/batch-C*.json); do
  python3 apply-descriptions.py --html work.html --edits $f
done
md5sum work.html      # (цепочка связей) 97d00dda4fc56bb93e9ae9b0f7cbdbe4
```

## Устройство папки

| Папка / файл | Что там |
|---|---|
| `apply-descriptions.py` | применяющий скрипт, один на все заходы |
| `data.json` | выжимка базы: 57 философов, 453 концепции, 1615 связей, 21 тип |
| `source/` | чистый исходник для пересборки |
| `spec/` | спецификация ревизии, редакция 4 |
| `dist/` | текущий правленый файл |
| `edits/` | 37 файлов правок, 1842 записи — единственный носитель изменений |
| `checks/` | проверки: `check-descriptions.mjs`, `tally.mjs`, `mutate.mjs`, `browser-check.mjs`, `browser-batch.mjs`, `known-exceptions.json`, `terms-whitelist.txt` |
| `reports/` | `report-summary.md` (сводный) и отчёты всех заходов: `report-00.md` … `report-C05.md` |
| `batches/<заход>/` | всё по одному заходу: `context.mjs`, `context.txt`, `texts.mjs`, `mkedits.mjs`, `list.md`, `report-head.md`, у ранних — `flags.json` |
| `tools/` | планирование межсистемного слоя (`outer-plan`, `sec54.md`), `package.json` |
| `scratch/` | разведочные скрипты первых замеров; в работе не нужны |

Папки заходов названы так же, как файлы правок (`03-scholastics`,
`M18-derrida-rorty`), а имена внутри единообразны — поэтому скрипт
любого захода годится образцом без переименований.

**Ревизия связей закончена.** Одночастных зачинов у `fix` не осталось: средняя
длина 232 знака против 239 у прочих описаний, двухчастный зачин у 79 % против
64 %. Прочее незакрытое — в разделе «Что можно сделать дальше» сводного отчёта.

## Начата новая ветвь: ревизия описаний КОНЦЕПЦИЙ

453 описания у 57 философов. Замер и спецификация готовы, содержательные заходы
не начаты.

- `spec/concepts-revision-spec.md` — критерии П1–П13, коридоры, раскладка на
  16 заходов, порядок работ (ревизия 1)
- `reports/concepts-audit.md` — замер исходного состояния
- `checks/check-concepts.mjs` — 20 проверок; `--json`, `--all`, `--only=код`
- `checks/mutate-concepts.mjs` — проверка проверок, 11 из 11
- `checks/audit-concepts.mjs` — развёрнутый замер
- `checks/selfcheck-batch.mjs` — самопроверка файла правок ДО применения;
  сверяет подписи и фразы захода СО ВСЕМ КОРПУСОМ
- `apply-concepts.py` — применение правок; адресация `id` + имя поля
- `checks/tally-concepts.mjs` — замер «до/после», запускать ОТДЕЛЬНОЙ командой
- `checks/browser-concepts.mjs` — окно концепции в Chrome

Итог замера: описаний без единого отказного дефекта — **29 из 453**; у **335**
единственный отказ — типографика; сверх неё отказ у **89**. Поэтому первым
делом идёт единый проход типографики (§ 8.1 спецификации), и лишь затем
шестнадцать содержательных заходов.

**Оснастка готова** (§ 8.3), **проход типографики выполнен** (T00): 438 правок,
422 концепции, отказов по базе **512 → 90**. `con.typography` чист.
## Начата третья ветвь: ревизия описаний ФИЛОСОФОВ

57 описаний, одно поле. Замер и спецификация готовы, заходы не начаты.

- `spec/philosophers-revision-spec.md` — критерии Ф1–Ф13, коридор объёма,
  раскладка на 6 заходов, поправочный заход Ф07 (§ 11), разбор вопроса о поле
  традиций (ревизия 3)
- `reports/philosophers-audit.md` — замер исходного состояния
- `checks/check-philosophers.mjs` — 18 проверок, коды `phi.*`
- `checks/mutate-philosophers.mjs` — проверка проверок, 13 из 13
- `checks/audit-philosophers.mjs` — развёрнутый замер

Итог замера: отказов **162**; чужого философа называют **55 из 57**, глагол
влияния — **все 57**; r(год рождения, объём) = **0,58** при r(число связей,
объём) = −0,08. Прохода типографики не требуется.

**Оснастка готова** (§ 8): `apply-philosophers.py` (адресация по `id`; сверяет
РАСШИФРОВАННЫЕ значения, ибо переводы строк существенны; шесть намеренных
ошибок пойманы поимённо), `checks/tally-philosophers.mjs`,
`checks/browser-philosophers.mjs` (проверена на порче),
`checks/selfcheck-batch-phil.mjs` (13 из 13 на порченом файле правок).
Chrome для Puppeteer —
`/home/claude/.cache/puppeteer/chrome/linux-131.0.6778.204/chrome-linux64/chrome`
(путь зашит в пробах; искать его надо по абсолютному пути, а не по `~`).

Раскладка ревизии 1 не сходилась со своим же счётом и заменена в ревизии 2:
**шесть заходов по 9–10** вместо пяти по 11–13.

**Заход Ф01 выполнен** (Пифагор — Августин, 10 философов, все `rewrite`).
Замер захода **55 → 0**, по базе отказов **162 → 132**. Файл:
`afbc84db4ebecbeb011f8763cc37283f`. Отчёт —
`batches/F01-antiquity/report-head.md` и `list.md`.
Самопроверка до применения поймала смешение кириллицы с латиницей внутри
слова — тот самый класс из К16, которого не видит ни одна из проверок `phi.*`.

**Заход Ф02 выполнен** (Ансельм — Беркли, 10 философов, все `rewrite`).
Замер захода **74 → 0**, по базе отказов **132 → 105**. Файл:
`6fe8e1a403b4877d8fc8f9c65b42f595`. Отчёт —
`batches/F02-anselm-berkeley/report-head.md` и `list.md`.
Первый заход, где сработал коридор объёма: `phi.length` 8 → 0, рекордсмен базы
сокращён вдвое (4619 → 2232). Снят иноязычный дублет — латынь в скобках при
русском термине, тот же класс, что снимался на ветви концепций.

**Заход Ф03 выполнен** (Юм — Кьеркегор, 9 философов, все `rewrite`).
Замер захода **74 → 0**, по базе отказов **105 → 82**. Файл:
`1e7b0aa10db54bd3628db117abf18ce0`. Отчёт —
`batches/F03-hume-kierkegaard/report-head.md` и `list.md`.
Все девять были вне коридора объёма, семь из девяти — вне коридора абзацев.
Опора закрыта почти вся: `phi.grounding` по базе 6 → **2**. `phi.era-bias`
повернула вниз (0,68 → 0,62).

**Заход Ф04 выполнен** (Маркс — Гуссерль, 9 философов, все `rewrite`).
Замер захода **78 → 0**, по базе отказов **82 → 56**. Файл:
`2fac042290251107dd6aa78f08ff7e73`. Отчёт —
`batches/F04-marx-husserl/report-head.md` и `list.md`.
Самый сильный заход по Ф8: у шестерых из девяти корпус собран не автором.
`phi.grounding` по базе закрыт до единицы (остался Дьюи). `phi.era-bias`
снижается второй заход подряд (0,62 → 0,55).

**Заход Ф05 выполнен** (Бергсон — Гадамер, 10 философов, все `rewrite`).
Замер захода **95 → 0** (рекорд ветви), по базе отказов **56 → 27**. Файл:
`59a8f1e5d3b21c5be7f44c9303f2f9b2`. Отчёт —
`batches/F05-bergson-gadamer/report-head.md` и `list.md`.
**`phi.grounding` закрыт по базе целиком**: труд назван у всех 57.
Проба в Chrome вскрыла дыру в себе самой — признак раздела взаимодействий
искался по списку основ, промахивавшемуся на Карнапе; переведён на заголовок
раздела и проверен на порче (0 из 10).

**ВЕТВЬ ФИЛОСОФОВ ЗАВЕРШЕНА ЦЕЛИКОМ** (заход Ф06, Лакан — Рорти, 9 философов).
Переписаны описания всех 57 философов за шесть заходов, все `rewrite`,
`keep` не выставлен ни разу. **Отказов по базе 162 → 0; все восемнадцать кодов
`phi.*` чисты.** Итоговый файл после Ф06: `3fc9f206bcb4d54e601b061a4d4f89ae`.
Сводный итог — `reports/philosophers-summary.md`.

Объём: крайние 1942/4619 → **1927/2598**, размах сжат вчетверо; труд назван
у 57 из 57; чужого философа не называет никто.

Завершение ветви потребовало **двух правок оснастки**: обратные мутации
`phi.self-name` и `phi.influence-verb` стали невозможны (насыщение снято) и
переведены в прямые, набор снова 13 из 13; сторож замера перестал считать
пустой вывод признаком обрыва потока. Оснастка, настроенная на состояние
«дефекты есть», врёт, когда дефектов не остаётся.

**Заход Ф07 (поправочный) выполнен**: выравнивание объёма, 13 правок `fix` —
первый и единственный `fix` на этой ветви. Тринадцати самым коротким описаниям
дописано по одному абзацу ПО СУЩЕСТВУ; прежний текст неприкосновенен, и это
проведено машинной заставой в `mkedits.mjs` (вставленный абзац снимается
обратно и требуется посимвольное совпадение с прежним текстом; застава
проверена на порче). Итог: **r(год рождения, объём) 0,82 → 0,17**, размах
671 → **331**, средние по заходам сравнялись. Файл:
**`71db03c785e06f6e0886fb12e87aa3bd`**. Отчёт —
`batches/F07-leveling/report-head.md`.

**Заход L01 — ПЕРВАЯ ПРАВКА ДАННЫХ, а не описаний.** Добавлено **девять
связей**: починен островок двойственного ума у Аристотеля (его система
распадалась надвое) и восемь рёбер, которых недоставало по существу. Связей
**1615 → 1624**. Файл: **`15a45d17e535e0f2c121a1b97245c2f0`**. Отчёт —
`batches/L01-newlinks/report-head.md` и `list.md`.

Систем, распадающихся на куски: 1 → **0**. Листьев (степень 1): 11 → **4** —
оставшиеся четыре держатся на одном ребре по существу. Точек сочленения графа
концепций: 9 → **3**.

**ВЕСА ВЫСТАВЛЯЮТСЯ ПО ЛЕГЕНДЕ ВЕСОВ** (≈4350), а не по раскладке корпуса:
у неё разные мерки для слоёв, и вес 3 внутри системы значит «несущая — без неё
система рассыпается», а между системами — «без этого отношения понятие не
возникло бы». Первая редакция захода этого не учла, и шесть весов из девяти
понижены с 3 на 2.

Появился **четвёртый применяющий скрипт**, `apply-newlinks.py`: адресация тою
же тройкой, но с обратным условием (тройки не должно быть); проверяет слой,
хронологию по полю `temporal` типа, вес и симметричность. Проверен пробой —
**восемь намеренных ошибок пойманы поимённо**. ГРАБЛЯ: последняя запись массива
`relations` стоит БЕЗ ЗАПЯТОЙ, и приписка склеивала два объекта; постпроверка
это поймала и файл не записала.

**НАЧАТО ПОЛЕ ТРАДИЦИЙ**: `spec/traditions-spec.md` — замеры, устройство,
черновик словаря из 22 традиций (покрытие 57 из 57) и шесть спорных мест,
требующих решения заказчика. ГЛАВНЫЙ ЗАМЕР: после ревизии описаний направление
называют лишь **6 описаний из 57** (прежде все), то есть поле стало НУЖНЕЕ, а
не наоборот. Автоматика традиций не находит: разбиение по историческому слою
даёт 48 против 9.

**ЗАХОД TR01 — ПОЛЕ ТРАДИЦИЙ ЗАВЕДЕНО.** Справочник `traditions` из 22 записей
и поле-массив у всех 57 философов (44 в одной традиции, 12 в двух, Хайдеггер в
трёх). Файл: **`0285a9414de0b1c8564a3fc747bd4559`**. Отчёт —
`batches/TR01-traditions/report-head.md`.

**ПРОВЕРКА ГРАФОМ ПРОЙДЕНА**: связи внутри традиций плотнее случайного ожидания
в 5,5 раза (34,0% против 6,2%); типологический слой лёг ПОПЕРЁК традиций на
93,7%. Оба замера возможны только после простановки.

Пятый применяющий скрипт `apply-traditions.py` (правит СРАЗУ ДВА места: массив
и поле у записей); проба из 8 намеренных ошибок — все пойманы. Новая проба
`browser-traditions.mjs`, проверена на порче. ГРАБЛЯ: разметка записей
неоднородна — у Ансельма поля на отдельных строках, у прочих 56 `id` и `name`
на одной; постпроверка это поймала и не дала записать файл.

**ЗАХОД V01 — РАЗДЕЛ ТРАДИЦИЙ В ОКНЕ ФИЛОСОФА.** Первая правка СЛОЯ ВЫВОДА.
Оформлен по образцу «Направлений работы» (метки `rubric-name-tooltip` с
пояснениями), стоит ПЕРВЫМ разделом — традиция есть данное о самом философе,
как и годы, а прочие разделы окно вычисляет. Заголовок согласован с числом.
Файл: **`f00be87108c1f142ebca59616b673b1d`**. Отчёт —
`batches/V01-traditions-view/report-head.md`.

Шестой применяющий скрипт `apply-view.py`: адресация ПО ЯКОРЮ (ровно одно
совпадение), главная постпроверка — правка вида НЕ ИМЕЕТ ПРАВА ТРОГАТЬ ДАННЫЕ.
**НАЙДЕНА ПУСТАЯ ПРОВЕРКА**: счёт скобок сверял вставку САМ С СОБОЙ и сработать
не мог; заменён разбором сценария через `node --check`.

**ЗАХОД V02 — ПРАВКА ТРАДИЦИЙ В СИСТЕМЕ РЕДАКТИРОВАНИЯ.** Сделана ПО ОБРАЗЦУ
рубрик у концепции: `<select multiple>` в форме, чтение через `selectedOptions`,
запись в обе ветви сохранения, предупреждение при пустом составе. Файл:
**`28ebd8758e60402490b063bf57da4ef7`**. Отчёт —
`batches/V02-traditions-edit/report-head.md`.

НАЙДЕНО ПО ДОРОГЕ: ветвь создания нового философа собирает запись перечислением
полей, и всякое новое поле надо вносить в неё руками — до правки новый философ
не получал традиций вовсе. Состав пишется В ПОРЯДКЕ СЛОВАРЯ (так воспроизводимо).

**ЗАХОД V03 — ОТБОР ПО ТРАДИЦИЯМ И ДВА НОВЫХ РЕЖИМА.** Принята МОДЕЛЬ А С
КНОПКАМИ: галочка традиции — обычный отбор («и» с философами), набор философов
она НЕ ТРОГАЕТ; правят его кнопки `=` («только эти») и `+` («добавить»), которые
состояния не хранят, потому порядок нажатий безразличен. Отсечённые традициями
философы не теряют галочек, а ПРИГЛУШАЮТСЯ. Новые режимы: «только внутри
выбранных традиций» и «только между выбранными традициями» — деление точное,
307 + 648 = 955 (все межфилософские связи), пересечение пусто. Файл:
**`c0de036ebfd5572631256fb411b75a5d`**. Отчёт —
`batches/V03-traditions-filter/report-head.md`.

**ЗАХОД V04 — ПЕРЕХОДЫ МЕЖДУ ТРАДИЦИЯМИ В ЦЕПОЧКЕ ПУТИ.** Стрелка при переходе
получает двойную засечку и заголовок; под цепочкой сводка, в окне цепочки шапка
и строка на сегмент. **СЧЁТ АБСОЛЮТНЫЙ** (по всем традициям философа), ибо поиск
идёт по ПОЛНОМУ графу и панель отбора в нём не участвует — сознательное
расхождение с режимами within/between. Переход = НЕТ НИ ОДНОЙ ОБЩЕЙ традиции;
«главная традиция» не выбирается. Различных традиций и переходов считаются
ПОРОЗНЬ: цепочка может выйти из традиции и вернуться. Файл:
**`1cf2b7e0be535086a0d3c5be3abaebe9`**. Отчёт —
`batches/V04-path-traditions/report-head.md`.

**ЗАХОД V05 — ТРАДИЦИИ В МЕТРИКАХ.** Правило: встраивать только то, что не
повторяет уже считаемое (мерилась корреляция рангов). СДЕЛАНО: новая метрика
**«Межтрадиционная мостовость»** (доля межтрадиционных среди внешних, по весам,
с порогом; корреляция с общей связностью 0,11 — почти ортогональна набору);
отбор пар по традициям в близких парах; поправлено ЛОЖНОЕ описание
синтетичности. ОТКЛОНЕНО ПО ЗАМЕРУ: традиции в синтетичности (0,84), в охвате
(0,89), в парадигмальном сдвиге (0,76), сходство философов по традициям
(вырождено: 1497 пар из 1596 дают ноль). Файл:
**`9784bea2c896c5434cf82219acb8bc0a`**. Отчёт —
`batches/V05-tradition-metrics/report-head.md`.

ГЛАВНАЯ ГРАБЛЯ: **метрики работают на ПРОЕКЦИИ записей**, а не на живых —
поля традиций там не было, и метрика дала 100 % у всех 293 концепций.
Поймало РАСПРЕДЕЛЕНИЕ (верх и низ списка совпали), а не проверки: формула была
внутренне согласна сама с собой. В пробу добавлена проверка на вырожденность.

**ЗАХОД V06 — ПЕРЕКЛЮЧАТЕЛЬ ВЗГЛЯДА НА ВЛИЯТЕЛЬНОСТЬ.** Три положения — вся /
внутри традиций / за пределы традиций — общие для вида концепций и для профилей
философов. **В положении «вся» итог В ТОЧНОСТИ ПРЕЖНИЙ**: разметка включается
только по запросу читателя. Генеративность рекурсивна, потому для двух других
взглядов обращённый граф строится заново; кеш разложен по взглядам. Расхождение
существенное: Спирмен «вся» против «за пределы» **0,651**, против «внутри»
**0,779**. Файл: **`253859c2e377f50a6bdfe881d804286f`**. Отчёт —
`batches/V06-influence-scope/report-head.md`.

ОТКЛОНЕНО ПО ЗАМЕРУ: тот же ход к революционности. Её формула берёт ТОЛЬКО тип
`oppose`, таких рёбер к более ранним всего 45 (14 своих, 31 чужая), и поправка
даёт Спирмен 0,996 — сдвигается один философ из 57.

**ЗАХОД V07 — ПОПРАВКИ МОСТОВОСТИ** (по двум замечаниям заказчика, оба верны).
(1) Метрики не было в `PROFILE_METRICS` — ВТОРОЙ случай того же упущения после
V05: новую величину надо вносить ВО ВСЕ списки явно, их девять. (2) Верх таблицы
решался порядком в массиве: **59 концепций упирались в потолок**, 33 из них на
трёх-четырёх связях. Сглаживание отвергнуто по замеру (убирает потолок, но
корреляция со степенью 0,43 при k=3 — губит ортогональность, ради которой
метрика и заводилась). Сделано: **порог 3 → 5** (на потолке 26, корреляция 0,13)
и **порядок равных по числу межтрадиционных связей**; о насыщении сказано в
пояснении прямо. Файл: **`f6007e7548137281aabb2a44d2a27526`**. Отчёт —
`batches/V07-bridging-fixes/report-head.md`.

ТРЕТЬЯ ПУСТАЯ ПРОВЕРКА ЗА РАБОТУ (после счёта скобок в V01 и тождества долей в
V05): проверка добора сортировала массив сама и сверяла со своей же сортировкой.
Переписана на разбор НАСТОЯЩЕГО вывода вида.

**Осталось незакрытым:**
не поправлено ложное срабатывание `phi.evaluative` на основе `выдающ`.

**Файл правится ТРЕМЯ цепочками.** Порядок обязателен:

```
cp source/philosophy_graph.SOURCE.html work.html
for f in $(ls edits/batch-0*.json) $(ls edits/batch-1*.json) \
         $(ls edits/batch-M*.json) $(ls edits/batch-C*.json); do
  python3 apply-descriptions.py --html work.html --edits $f
done
for f in $(ls edits/batch-T*.json) $(ls edits/batch-K*.json); do
  python3 apply-concepts.py --html work.html --edits $f
done
for f in $(ls edits/batch-F*.json); do
  python3 apply-philosophers.py --html work.html --edits $f
done
for f in $(ls edits/batch-L*.json); do
  python3 apply-newlinks.py --html work.html --edits $f
done
python3 apply-traditions.py --html work.html \
                            --edits edits/batch-TR01-traditions.json
for f in $(ls edits/batch-V*.json); do
  python3 apply-view.py --html work.html --edits $f
done
```

**ВЕТВЬ КОНЦЕПЦИЙ ЗАВЕРШЕНА ЦЕЛИКОМ.** Переписаны описания всех 453
концепций (906 полей): один проход типографики и шестнадцать содержательных
заходов. Отказов по базе **512 → 0**. Сводный итог —
`reports/concepts-summary.md`.

**Файл правится двумя цепочками.** Порядок обязателен: сперва связи, затем
концепции.

```
cp source/philosophy_graph.SOURCE.html work.html
for f in $(ls edits/batch-0*.json) $(ls edits/batch-1*.json) \
         $(ls edits/batch-M*.json) $(ls edits/batch-C*.json); do
  python3 apply-descriptions.py --html work.html --edits $f
done
for f in $(ls edits/batch-T*.json) $(ls edits/batch-K*.json); do
  python3 apply-concepts.py --html work.html --edits $f
done
md5sum work.html      # 26d9eea9ea44e65863df42c0fd6d5b61
```

## Восстановление окружения

```bash
cp tools/package.json tools/package-lock.json .
npm install        # puppeteer для browser-check
```

Chrome для Puppeteer в прежней среде лежал в
`~/.cache/puppeteer/chrome/linux-131.0.6778.204/chrome-linux64/chrome`.
